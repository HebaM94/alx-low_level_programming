#include "main.h"
/**
* mul - a function that multiplies 2 integers
* @a: first number to be multiplied
* @b: second number to be multiplied
* Return: r (result of mul)
*/
int mul(int a, int b)
{
int r;
r = a * b;
return (r);
}
